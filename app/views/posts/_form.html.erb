<% if @post.errors.any? %>
	<% @post.errors.full_messages.each do |msg| %>
 	<% end %>
<%  end %>




		<%= simple_form_for @post do |f| %>
		  
		  	<%= f.input :title, label: "Title", placeholder: "Title of post" %>
		  
		  	<%= f.input :content, placeholder: "Body of post", :as => :text, :input_html => {:rows => 10} %> 
		 		  	
		  	<%= f.button :submit, class: "btn btn-primary" %>

		<% end %>
	